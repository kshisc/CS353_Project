# CS353_Project
A multithreaded HTTP/1.1 web server written in C++ that handles concurrent client connections using TCP/IP sockets. Implements request parsing, static file serving, thread synchronization with mutexes and condition variables, and a reaper thread for safe resource cleanup. Includes logging and connection monitoring for debugging and performance analysis.
